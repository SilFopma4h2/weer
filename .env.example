# Default location (latitude, longitude)
DEFAULT_LAT=52.3676
DEFAULT_LON=4.9041

# Cache settings
CACHE_DURATION=600

# Local server settings (used by `python app.py`)
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Ngrok: automatically create a public tunnel when running `python app.py`
ENABLE_NGROK=true